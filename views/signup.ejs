<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <meta name="csrf-token" content="<%= csrfToken %>"/>
    <link href="./css/styles.css" rel="stylesheet" />
    <title><%= title %></title>
    </head>
    <body>
        <div class="grid grid-cols-6">
            <div class="col-start-3 col-span-2">
                <%- include('header.ejs') %>
                <h6 class="py-4">Signup as a new user</h6>
                <form action="/users" method="POST">
                    <input name="_csrf" type="hidden" value="<%= csrfToken %>">
                    <div class="py-3">
                        <label for="firstName">firstName:</label>
                        <input type="text" name="firstName" class="border border-gray-300 rounded text-gray-900 w-full p-2 text-sm">
                    </div>
                    <div class="py-3">
                        <label for="lastName">lastName:</label>
                        <input type="text" name="lastName" class="border border-gray-300 rounded text-gray-900 w-full p-2 text-sm">
                    </div>
                    <div class="py-3">
                        <label for="email">email:</label>
                        <input type="email" name="email" class="border border-gray-300 rounded text-gray-900 w-full p-2 text-sm">
                    </div>
                    <div class="py-3">
                        <label for="password">password:</label>
                        <input type="password" name="password" class="border border-gray-300 rounded text-gray-900 w-full p-2 text-sm">
                    </div>
                    <div class="py-3">
                        <button type="submit" class="bg-green-500 text-black rounded px-5 py-2 font-medium mr-2 mb-2 hover:bg-green-800">Sign Up</button>
                    </div>
                    
                </form>
            </div>
        </div> 
    </body>
</html>
